import { FiltroEmpleadoPipe } from './filtroEmpleado.pipe';

describe('FiltroEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
